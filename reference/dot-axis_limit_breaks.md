# Helps make nice axis limit breaks

Helps make nice axis limit breaks

## Usage

``` r
.axis_limit_breaks(plot.limits, break.size = NULL)
```

## Arguments

- plot.limits:

  vector containing plot limits

- break.size:

  space between breaks
