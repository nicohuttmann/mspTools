# Returns a randomly generated vector of colors sorted by hue

Returns a randomly generated vector of colors sorted by hue

## Usage

``` r
.rcol(n = 1, seed = 42)
```

## Arguments

- n:

  number of colours to return

- seed:

  random seed, so the colours are reproducible
